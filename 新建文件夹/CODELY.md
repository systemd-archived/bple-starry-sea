

## Codely Structured Memories

### User

### Feedback
- [2026-08-26 15:54:20] 不要在未明确确认前改动项目核心启动/共享文件(如 INSettings.cs 的 InitializeSettings); **也不要再尝试任何 EventSystem 去重修复**。**Why:** 我为修"多 EventSystem-输入框不能输入"尝试了两种方案——①在 INSettings.InitializeSettings 加"已存在则不再实例化"守卫、②新增独立 INEventSystemGuard.cs 每帧销毁多余 EventSystem——均已两次/每次导致"连设置都进不去了", 用户都要求完全还原。**How to apply:** 排查/修复涉及核心启动流程或 EventSystem 的问题时, 先只做读取诊断并向用户确认方案, 不要直接写代码; 即便用"独立文件不碰 INSettings"的做法也仍会破坏设置入口, 不要再用。改后务必让用户实测设置/主流程仍能进入。
- [2026-08-26 17:53:31] **每次修改任何文件都必须先备份, 且备份可无限叠加, 新备份绝不能覆盖旧备份; 禁止删除任何备份。** 范围涵盖**一切文件**: C#(.cs)、prefab(.prefab)、场景(.unity/.scene)、JSON/ScriptableObject(.asset/.json)、TextAsset、.meta、.mat 等, 改哪个就先备份哪个。**Why:** 用户在多 EventSystem/INVersionSelector/AddonVideoPlayer.prefab 修复中反复要求"先备份再改"，并多次强调"每一次修改都要备份，禁止删除备份"、"备份可无限叠加，新备份不能覆盖旧备份"、"以后不管改哪里都要备份"；曾因误移除 AddonVideoPlayer.prefab 的 MeshRenderer/MeshFilter 需靠备份恢复。**How to apply:** 所有写操作(包括改 prefab 资产)前先 Copy-Item 备份，文件名用可递增唯一标识(如 .bak_日期 或 .bak_序号)确保不覆盖旧备份；备份永不清除、长期保留；每次改动前核对已备份。
- [2026-08-26 22:30:09] [2026-08-26] **安卓端文件上传：不要依赖"唤出系统文件选择器/文件管理"**(ACTION_OPEN_DOCUMENT/文件对话框)，安卓唤不出。**改用"手动粘贴/输入文件路径"来加载**(背景图/视频、addon 等)。**Why:** 我为上传加了 Java 桥(FileDialogBridge.java)+INFilePicker(Android ACTION_OPEN_DOCUMENT)+Manifest 注册，但安卓端无法唤出文件管理；用户明确"虽然无法唤出文件管理，但可以通过粘贴路径实现"。**How to apply:** 背景上传(UplImage/背景路径输入栏)以 m_pathInput 文本框为唯一输入源，用户粘贴真实路径后校验/加载；粘贴的路径在安卓需能读(如 /storage/emulated/0/... 需 MANAGE_EXTERNAL_STORAGE，或应用目录)。跨平台文件选择 INFilePicker 保留(桌面可用)，但安卓上传展示应以路径输入为主，不强求唤文件选择器。
- [2026-08-28 14:23:05] INLocalizationData.json 重复 key 会导致设置入口崩溃。添加本地化条目时插入新条目后，必须检查同文件内是否已存在相同 id（备份恢复也可能带入重复）。**Why:** 用户曾因重复 GateExtensionSpeed key 导致 ArgumentException 设置界面无法进入。**How to apply:** 改完 INLocalizationData.json 后 grep 检查无重复 id 再编译。
- [2026-08-28 22:17:40] 用备份还原文件后必须自动编译（start_compilation_pipeline），否则改动不会生效。**Why:** 用户曾因恢复备份后没编译，误以为改动没生效，要求还原更早的备份。**How to apply:** 每次 Copy-Item 恢复备份后，立即 stop play mode（如需要）+ import（如需要）+ start_compilation_pipeline。**⚠️部分恢复陷阱(2026-08-28)**：恢复 A 文件的备份时，A 的旧版引用的属性可能在 B 文件的当前版已被删除（或反之）。必须确保所有关联文件版本一致——StarSeaSettings.cs/INSettingsInterface.cs/INLocalizationData.json/ElectricalSystem.cs 四文件必须匹配同一"时代"的备份。

- [2026-08-28 19:12:36] PointChargePart 旋转方案迭代教训(2026-08-28): 用 `m_autoAlign=Rotate` + `m_isUlMode` 字段的方案不稳定(被 Contraption.Flip 的 m_hasSuperMagnet 块无条件覆盖 m_autoAlign=ul)，用 `IsCustomRotated()` + `m_autoAlign=ul` 的方案也不稳定(被用户报告"打回")。**最终正确方案：完全不依赖 ul，原生4朝向**——`IsCustomRotated()=true`(永远)+`SetFlipped()`空+`GetConnectionDirection()=Any`(永远)+modulo4。**Why:** ul 是特殊 AutoAlignType 值(-114514)，Contraption.Flip 的超级磁铁代码块会无条件覆盖它，导致依赖 ul 的方案在特定场景下失效。**How to apply:** 新增旋转类部件时，若需多朝向支持，直接原生实现 IsCustomRotated/SetRotation，不依赖特定 AutoAlignType。
- [2026-08-29 14:29:04] Unity sprite的pivot(通常左上角)≠视觉中心。用ConfigurableJoint连接两个sprite物体时：①对齐位置用`renderer.bounds.center`(世界坐标视觉中心)而非`transform.position`(pivot)；②旋转轴通过`joint.anchor=transform.InverseTransformPoint(renderer.bounds.center)`+`joint.connectedAnchor`同理，否则旋转围绕pivot转而非视觉中心。**Why:** PointChargePart连接时发现圆环绕着左上角转而非正中间转。**How to apply:** 任何sprite物理关节(旋转/弹簧等)都需设anchor到bounds.center，否则旋转轴在pivot。
- [2026-08-29 15:19:20] exec_editor_script 在本项目持续报 "The type initializer for 'Codely.Microsoft.CodeAnalysis.Scripting.ScriptOptions' threw an exception"，根因同已知问题：Assets/Plugins/UnityEngine.UI.dll 与 com.unity.ugui asmdef 冲突破坏 Roslyn 引用解析。**How to apply:** 不能用 exec_editor_script 做 C# 自动化，改用 replace/write_file 直接编辑 .cs 文件 + unity_editor start_compilation_pipeline 编译。
- [2026-08-29 19:14:04] Unity `GetComponents<Collider>()` 只返回当前 GameObject 上的碰撞体，不包含子对象碰撞体。伞(canopy)、南瓜等部件碰撞体在子 GameObject 上，必须用 `GetComponentsInChildren<Collider>()` 才能覆盖。**Why:** 用户反馈伞/南瓜仍碰撞，排查发现这些部件碰撞体在子对象。**How to apply:** 任何需要"覆盖部件所有碰撞体"的场景（碰撞忽略、物理检测等），用 `GetComponentsInChildren` 而非 `GetComponents`。
- [2026-08-29 19:23:50] Unity `GetComponents<Collider>()` 只返回当前 GameObject 上的碰撞体，不包含子对象碰撞体。伞(canopy)、南瓜等部件碰撞体在子 GameObject 上，必须用 `GetComponentsInChildren<Collider>()` 才能覆盖。**Why:** 用户反馈伞/南瓜仍碰撞，排查发现这些部件碰撞体在子对象。**How to apply:** 任何需要"覆盖部件所有碰撞体"的场景（碰撞忽略、物理检测等），用 `GetComponentsInChildren` 而非 `GetComponents`。
- [2026-08-30 22:51:54] 版本号更新模式: 用户频繁要求更新打包版本号（如 901-4→901-5→901-6→901-7），每次改 BuildAndroidApk.cs 和 BuildWindows.cs 的输出文件名即可，不改 bundleVersionCode/bundleVersion。APK 和 Windows 版本号应保持同步。**Why:** 用户习惯快速迭代版本号。**How to apply:** 改版本号时两个脚本都要改，备份后再改，编译后执行打包。

### Project
- [2026-08-26 13:59:05] 侧栏(INApplicationInterface): 插件系统(addon, AppInterface_AddonSystem)与"更多自定义"(AppInterface_MoreCustom)是两个独立入口, 不可合并。更多自定义承载"自定义背景"页(INCustomBackgroundInterface), 布局复用 PartAdjustInterface/星海设置模板(Scroll View可滚动 + SettingsGroupTemplate栏 + SettingsItemTemplate行), 现含5栏: 开启背景/播放-暂停(开关)、背景路径输入栏(支持图片或视频路径)、视频音量(int 0~1)、跳转时间秒(VideoPlayer.time)。上传/清除按钮在Scroll View外ButtonGroup。图片背景用CreateBackground+backgroundZ=6(对齐addon main.cs Vector3(0,0,6); 视频用CameraFarPlane+Camera.main不需z)。


- [2026-08-26 13:18:55] C# 脚本引擎(execute_csharp_script)在本项目无法用于运行时验证: 报 "Can't create a metadata reference to an assembly without location", 根因是项目既有 Assets/Plugins/UnityEngine.UI.dll 与 com.unity.ugui asmdef 同名冲突(console 有 AssemblyValidation 警告)。**Why:** 该插件 DLL 无 location 加载, 破坏 Codely Roslyn 引用解析。**How to apply:** 需要运行时/play 截图验证时, 先处理该 DLL(重命名)或改用文件级确定性核对(读 prefab YAML/JSON)+编译检查。
- [2026-08-26 13:38:15] 本项目直接编辑磁盘 YAML(prefab/.json/ScriptableObject的.asset/TextAsset) 后, 运行时不会立刻生效: refreshMode=debounced, play 用的是进入前导入的缓存。**Why:** 用户曾因改了 INApplicationInterface.prefab 和 INLocalizationData.json 未 reimport 导致改动"消失"/"按钮显示空格"。**How to apply:** 改完这类资产必须用 unity_asset action=import 对该文件重导入(或在编辑器停留让其自动 import), 再重新进 play 验证。
- [2026-08-26 13:38:15] INLocalization.GetText(id) 用字典索引 m_localization[id], 若本地化 key 缺失会用字典索引发异常→ UITextLocale 显示空/空白按钮文字(而非回退文本)。**Why:** "更多自定义"入口曾显示空格, 根因是 INLocalizationData.json 加了 AppInterface_MoreCustom 后未重导入, 运行时字典缺失该 key。**How to apply:** 排查 UI 按钮/文本空白时, 先确认对应本地化 key 在 INLocalizationData.json 存在 且 已对 TextAsset 重导入; 侧栏入口的 RelatedObject 名字要与 m_elements 元素名一致(经 FindElement 查找)。
- [2026-08-26 13:59:18] 视频背景(INCustomBackgroundInterface)两个坑: ①视频文件不可拷成固定名 uploaded.png —— VideoPlayer 按扩展名猜格式, 视频被改名.png 会报 "VideoPlayer cannot play url"; 应视频直接用原始路径播放(保留.mp4/.mov扩展名), 仅图片拷到 uploaded.png 持久化。②视频只有声音无画面: VideoRenderMode.MaterialOverride + Quad+材质 不可靠; 改用 VideoRenderMode.CameraFarPlane + targetCamera=Camera.main 作为远景背景才能稳定显示画面。
- [2026-08-26 17:30:09] INCustomBackgroundInterface 实现演进(2026-08-26 多轮, 末轮见 17:07:02 与 17:30): 曾用 **addon 播放器**(INAddonManager.CreateVideoPlayer/CreateBackground, LocationMode.CameraAndScreen)。曾用 `CustomBackgroundScale:MonoBehaviour` 挂在背景 GameObject 上、`LateUpdate` 读静态 `CustomBackgroundConfig` 覆盖 transform——**但该驱动组件最后已被用户明确移除**(见 17:30 更), 因为它每帧把背景从 addon 默认 (0,0) 挪到相机前方/加偏移/旋转, 用户因此在 (0,0) 找不到 AddonVideoPlayer(Clone)。**How to apply:** 不要再用"每帧覆盖 addon 背景 transform 的持久组件"来驱动缩放/偏移/旋转; 若需这类功能, 先向用户确认是否接受背景脱离 (0,0)。面板字段与 CustomBackgroundConfig 静态容器现仅存储值, 不再驱动 transform。










- [2026-08-26 17:48:45] **"所有输入框不能输入"的最终根因已定位(2026-08-26)：不是背景代码, 而是项目 `INSettings.InitializeSettings()`(INSettings.cs)每次都无条件 `DontDestroyOnLoad(Instantiate(INUnity.LoadGameObject("EventSystem")))`, 多次进出设置/版本流程会累积出多个 EventSystem → console 刷 `There are 2 event systems in the scene` → uGUI InputField 依赖 EventSystem.current 路由事件, 多个 EventSystem 打架导致所有输入框无法输入。**多 EventSystem 对象会跨场景残留, 回退背景代码无用。** **"Fix须在运行时只保留一个 EventSystem"结论已被证伪：两种方案(①InitializeSettings 加防重守卫、②独立 INEventSystemGuard.cs 每帧销毁多余 EventSystem)均导致"进不去设置/入口损坏", 用户两次要求完全还原。** 结论: 当前不要动 EventSystem/InitializeSettings 启动逻辑。**背景面板栏目(2026-08-26 末轮): 开启背景/背景路径/视频音量/自定义缩放/X轴拉伸/Y轴拉伸/X轴偏移/Y轴偏移/Z轴距离(默认16)/时间跳转(秒)/旋转角度(度,0~360无限制)/播放循环/播放暂停/静音建造BGM/静音游戏BGM。** ⚠️最后重要调整: 用户因"AddonVideoPlayer(Clone) 在 (0,0) 找不到"而选择**完全移除 CustomBackgroundScale 驱动组件**(EnsureScaleDriver 不再 AddComponent, 该类已删除), 让背景停回 addon 默认位置。****最新指令(2026-08-26 17:48): 恢复 addon 播放器(INAddonManager.CreateVideoPlayer/`avp.Player`)为调用核心, 并已恢复 AddonVideoPlayer.prefab 上的 MeshRenderer/MeshFilter(此前误移除导致异常)。**How to apply:** ①URL 源视频 clip 恒 null, 判就绪用 isPrepared+(frameCount>0||length>0); ②addon 播放器底层 VideoPlayer 是 `avp.Player`, 修复须在保留 addon 调用基础上做, 不动 AddonVideoPlayer(Clone) 本体组件; ③排查全局"输入框都不能输入"时先查 EventSystem 数量; ④EventSystem 去重修复会破坏设置入口(已证两次), 不要再用; ⑤任何"每帧覆盖 addon 背景 transform"的驱动组件会把背景从 addon 默认 (0,0) 挪走, 用户明确不想要, 不要再用这种驱动。



- [2026-08-26 17:48:31] **⚠️已推翻修正**：曾把"世界坐标(0,0)白色方块"归因于 `Assets/GameObject/AddonVideoPlayer.prefab` 自带的 MeshRenderer+MeshFilter，并移除这两个组件——**但该判断错误，移除后导致 addon 播放器调用出现异常**。用户明确指令"改回来 AddonVideoPlayer(Clone)"，已从备份 `.bak_20260826` 恢复含 MeshRenderer/MeshFilter 的原始 prefab 并 reimport。**How to apply(修正后):** `AddonVideoPlayer.prefab` 上的 MeshRenderer/MeshFilter 是原厂组件**不要移除**; 若 addon 背景/视频在原点出现白色方块，先确认是否只是 addon 正常占位或材质/相机设置，不要擅自删 prefab 组件，改动前先用户确认并备份；addon 播放器调用(CreateVideoPlayer/`avp.Player`)是核心，修复问题要在保留该调用基础上做，不动 AddonVideoPlayer(Clone) 本体。

- [2026-08-29 12:01:35] **INCustomBackgroundInterface 当前状态(2026-08-29更新)**：addon播放器(CreateVideoPlayer/CreateBackground)+`CustomBackgroundScale`每帧驱动+`CustomBackgroundConfig`静态+Record持久化。**Z轴距离默认值=4**。LocationMode同前。**按钮组：** 上传背景/清除背景/快速填充。**分组1栏目=** 开启背景/背景路径/视频音量/透明度/自定义缩放/摄像机自适应/XY拉伸/XY偏移/绝对坐标/旋转/Z轴距离(默认4)/播放循环/**视频播放速度(默认1)**/**跳转时间(秒)**(支持纯秒/百分比如50%/时分秒如1:30:00/分秒如5:30，中英文冒号均支持)/**播放进度**(只读文本MM:SS.s/总时间(百分比%精确0.1)+视觉进度条Slider动画，每0.1秒更新，纯显示无交互)/播放暂停。**分组2"音量设置"=** 建造界面音量/运行界面音量/音效音量/关卡选择音乐/进入游戏音量。**已删除：** "加速变调"toggle(音调控制在addon Direct模式不可行，已验证并删除)。**涉及文件：** INCustomBackgroundInterface.cs。
















- [2026-08-26 18:37:48] 多EventSystem输入框失效根因已精确定位(2026-08-26实测): 进入设置即2个EventSystem=①INVersionSelector.prefab内嵌EventSystem子对象(fileID 1985305603660343, 版本界面随prefab实例化就有) + ②INSettings.InitializeSettings():642 点进入时 DontDestroyOnLoad(Instantiate(独立prefab EventSystem))。单次进设置即2个(非反复进出累积)。要减到1必须消除其中一处创建, 唯一可靠控制点在INSettings.cs:642; "去内嵌+另建持久EventSystem"仍会因②新建第二个而失效。排除: 场景无EventSystem、全项目仅INSettings.cs:642按名加载"EventSystem"。用户决定暂不修, 完整方案存于 多EventSystem输入框失效根因分析.md, 后续需用户授权才可碰INSettings.cs。
- [2026-08-26 21:28:34] [2026-08-26] 本项目**安卓打包正确方式 = Mono 后端 + 仅 armeabi-v7a(32位)**（参照用户提供的 BPLE-2022.1.9.3.apk 验证：libmonodwgc-2.0.so 无 libil2cpp.so）。**Why:** 用 IL2CPP+ARM64 会因 Managed Stripping 把设置界面反射属性裁掉→设置界面空/无输入栏（PartSettings.Apply/INSettingsInterface 用 type.GetProperties() 反射），且打包慢/易异常。改为 Mono 后设置正常、打包快且稳定(约76MB)。**How to apply:** 打包 APK 保持 Mono2x + ARMv7，勿改回 IL2CPP/ARM64。应用名"新创unity星海"、包名 com.star.tech.build、版本号注意用户会临时改(如 2022.1.901 / 26.8.24.16)。打包 APK 前先 Rebuild Android AssetBundle，再 Tools/Build/Build Android APK。切平台(SwitchActiveBuildTarget)会触发域重载致 Unity heartbeat 卡死/菜单"没触发"，属正常，需刷新 Bridge 并等待。
- [2026-08-26 22:09:22] [2026-08-26] 安卓端"无法上传文件"(背景/视频上传、addon包导入)根因：项目用 Windows 专用 `ShellFileDialogs.FileOpenDialog.SelectSingleSelectDialog`(COM IFileDialog)，安卓无此 API。已用跨平台方案修复(编译通过)：新建 `INFilePicker.cs`(Windows用ShellFileDialogs、安卓用 `com.innovation.filedialog.FileDialogBridge` 的系统 `ACTION_OPEN_DOCUMENT` 选择器+复制选中文件到 cacheDir+静态结果由 C# 协程轮询回调)、新增 `Assets/Plugins/Android/com/innovation/filedialog/FileDialogBridge.java`(内含 FileDialogActivity)、AndroidManifest 注册该 Activity；并把 `INCustomBackgroundInterface.UploadImage`/`INAddonInterface.OpenFileDialog` 改为调 `INFilePicker.PickFile(desc,ext,onPicked)` 回调式。**改前已备份**：INCustomBackgroundInterface.cs.bak_20260826_filedialog、INAddonInterface.cs.bak_20260826_filedialog、AndroidManifest.xml.bak_20260826_filedialog。**注意：Java桥+Manifest 需重新打安卓APK 才在设备生效**；上传的视频文件名需保留扩展名(AndroidFileDialogBridge 复制时用 OpenableColumns.DISPLAY_NAME)；安卓 MIME 由 filterExt 映射。首版本 Java `OpenFile` 用 Unity activity startActivity 不可行，改用独立 FileDialogActivity + 静态结果**轮询**，C# 用 CoroutineRunner.Instance.StartCoroutine 轮询 `IsReady/GetResult`。
- [2026-08-27 15:09:36] [2026-08-27 15:15:00] **IN 设置界面/星海设置重置按钮修复(Android 重新设置失效)**：背景是项目共享设置系统(INSettingsInterface.cs 的 Reset + Binding/DependencyProperty 双层绑定：A=源属性→UI 代理 m_property，B=代理→源)。用户反馈安卓端"点重置后再次设置失效，退出重进才好"。**修复方式(2026-08-27)：** 在 INSettingsInterface.cs ——①`SettingsItem<T>` 新增 `m_getter` 字段并在 Bind 时记录、新增 `SyncFromSource()`(SetValue(m_getter()) 从源重读入代理并回显)；②基类抽象 `SettingsItem` 新增 `public abstract void SyncFromSource();`，`SettingsItem<T>.SyncFromSource` 用 `override`；③`Reset()` 末尾在 `m_dirty=true` 后对每个 Group 的每个 Item 先 `SyncFromSource()` 再 `Render()`，确保重置后源=默认=代理=UI 一致，后续编辑能正确比较、写入源并生效(不必退出重进)。未改动 INUserSettings.Reset/INSettings/Binding 入口，未自动 Save(仍走保存按钮m_dirty置*)，未删除任何新增项。编译通过()。备份=`INSettingsInterface.cs.bak_20260826_before_reset_sync_fix`。注意：涉及共享设置核心，若后续回归"设置入口/编辑失效"先从本改动复盘。
- [2026-08-27 16:02:32] INCustomBackgroundInterface 最终修正(2026-08-27多次往返)。**快速填充(用户确认的最终行为)：** `m_stashedScaleX/m_stashedScaleY`(初始1)=原版XY拉伸暂存；`FillShortcut` 首次按=①先暂存当前(用户手设的)XY拉伸到 m_stashedScaleX/Y、②再关摄像机自适应+启绝对坐标+仍把背景当前实际localScale抄进XY拉伸+当前世界位置抄进绝对坐标X/Y(不改Z)；再次按=恢复暂存的**原版**XY拉伸+开摄像机自适应+关绝对坐标(**不是**还原1,1)。**视频自定义缩放 X=Y=1=原版比例无拉伸(根因与正确修法)：** 驱动组件 `CustomBackgroundScale.LateUpdate` 读的是**静态** `CustomBackgroundConfig.MediaAspect`, 而 `m_mediaAspect` 是实例字段; 单靠 Update() 推 `m_mediaAspect` 不会到静态配置(静态仍0)→ 驱动兜底用 camAspect → 相机/GameView 是方屏时视频被拉成1:1。**修法:** Update() 里用 `m_videoPlayer.clip`(或URL源时 clip==null 用 `m_videoPlayer.width/height`) 推 m_mediaAspect 后, 必须**立即** `if(m_mediaAspect>0f) CustomBackgroundConfig.MediaAspect=m_mediaAspect;` 让驱动读到真实视频比例 → `localScale=(viewH*mediaAspect*ScaleX, viewH*ScaleY,1)` 在 SX=SY=1 时保持原版比例无拉伸。**三档音量(建造界面音量/运行界面音量/音效音量)在更新备份 `...bak_20260827_before_fill_value`(47039B) 及更晚版本里才有, 更早的 `z19_fill_before_buildrun_volume`(39212B) 没有——还原须选对备份, 否则功能性设置组会丢失。**当前文件=从 `before_fill_value` 恢复+补 aspect-push, 已含全部栏目与三档音量(音效音量经 AudioManager.ApplySoundVolume, UserSettings "SoundVolume")。快速填充旧备份(`...bak_20260827_before_fill_value`,`...bak_20260827_160056_before_restore_volumes`)均保留。
- [2026-08-28 10:17:09] **星海设置新增自定义游戏参数的完整模式(2026-08-27)**: StarSeaSettings.cs(字段+属性+构造默认+Update拷贝) → INSettingsInterface.cs `GenerateStarSeaGroup` 注册属性名 → INLocalizationData.json 加 locale 条目 → 业务脚本通过 `INUserSettings.Instance?.StarSeaSettings?.XxxProperty ?? 默认值` 读取。**⚠️重要: 本地化标签与属性名是反过来的!** GateWidthCoefficient → 显示"长度系数"；GateLengthCoefficients → 显示"宽度系数"。**MechanicalGate 已实现7项+1项碰撞修复**: GateWidthCoefficient(float,1,显示"长度系数")/GateLengthCoefficients(string,"1,1,1,1,1",显示"宽度系数",5种门逗号分隔)/GateOpacity(0.8)/GateDefaultLengths(string,"1,2,4,8",5种门固定长度)/GateAdjustableRange(string,"0.1,8,0.1",可调门min/step/max)/GateExtensionSpeed(0.2,伸出速度)。**碰撞修复(2026-08-27)：** FixedUpd`late中根据`value > 0`动态开关`m_gateCollider.enabled`——关闭动画播放期间门还在外面碰撞保留,完全收回(value==0)后才去除碰撞。`Initialize`中也根据`m_enabled`初始状态设置。**Why:** 机械门有5种(4不可调+1可调),用户要求默认长度/宽度系数按逗号分隔给每种门独立设置。**How to apply:** 后续新增类似游戏参数沿用此四文件模式;修改属性前先确认本地化标签与属性名的对应关系。
- [2026-08-28 13:58:36] INCustomBackgroundInterface 自动切换最终方案(2026-08-28更新): LoadRecord 中当 record.Enabled=true 时，跳过初始 RefreshBackground，直接 Invoke(nameof(DelayedOpenBackground), 0.03f) 延迟创建。DelayedOpenBackground 方法设置 toggle IsOn=true + OnToggleChanged(true)。**Why:** ①协程在 yield return null 后被杀（多次测试确认）；②先 RefreshBackground 再 Destroy 再创建会产生两个 AddonBackground(Clone)，多余的停在(0,0,0)且不被 CustomBackgroundScale 驱动；③目标是关闭再打开背景开关（m_enableToggle），不是自定义缩放（m_customScale）。**How to apply:** 涉及 INCustomBackgroundInterface 的延迟操作用 Invoke 而非协程；LoadRecord 要延迟创建时跳过初始 RefreshBackground 避免双对象。

- [2026-08-28 13:58:24] 星海设置新增音乐相关参数(2026-08-28): UseLevelSelectionMusic(bool,默认true)控制关卡选择界面用level_selection.ogg还是MusicTheme；EntryMusicVolume(float,0~1,默认1)控制刚进入游戏时主菜单音乐音量。四个文件改动: StarSeaSettings.cs(字段+属性+构造默认+Update)、INSettingsInterface.cs(Music分组)、INLocalizationData.json(3条本地化)、MusicManager.cs(override/非override两个ReceiveLoadingLevelEvent均读取StarSeaSettings)。
- [2026-08-28 14:22:53] 音乐设置已从星海设置(StarSeaSettings)移到更多自定义(INCustomBackgroundInterface)的音量设置分组(2026-08-28)。字段: UseLevelSelectionMusic(bool,默认true)和EntryMusicVolume(float,0~1,默认1)。通过 UserSettings.GetBool/GetFloat 传递给 MusicManager 读取。**Why:** 用户要求这些设置归入"更多自定义"而非星海设置。**How to apply:** 音乐相关设置修改 INCustomBackgroundInterface + MusicManager，不动 StarSeaSettings.cs/INSettingsInterface.cs/INLocalizationData.json。
- [2026-08-28 15:24:00] Shader 缓存损坏导致 ff00ff 边缘(2026-08-28): `_Custom/Unlit_Color_Geometry` shader 出现 `Importer(ShaderImporter) generated inconsistent result` 警告，CombinedMesh__CustomUnlit_Color_Geometry 等场景网格边缘变品红。重启电脑/重进工程/Reimport 均无效。**根因:** 多次编译/域重载后 Library 缓存损坏。**修复:** 删除整个 Library 文件夹让 Unity 完全重建。**How to apply:** 出现场景网格 ff00ff 品红边缘时先检查 shader compile 是否有 inconsistent result 警告，有则删 Library 重建。
- [2026-08-28 16:18:40] e2dCurve.mat 材质文件被截断导致 ff00ff 边缘(2026-08-28): e2dCurve.mat/e2dCurve_0.mat/e2dCurve_1.mat 从正常1412字节被截断为41字节(只剩YAML header)，导致场景中 _curve 对象 MeshRenderer 的 sharedMaterial 运行时为 null。根因不明（非代码改动导致），从参考工程(BPLE 2022.1.9 zip)恢复后解决。**How to apply:** 出现 ff00ff 品红材质时，先检查 Materials 目录下 .mat 文件大小是否异常（<100字节），再与参考工程对比恢复。
- [2026-08-28 18:18:56] StarSeaSettings 新增 `StableMultiblock`(稳定多功能块) bool 属性，默认 true。控制 DigitalDisplay 正向0模式 `UpdateTargeting` 的目标追踪逻辑：ON=新逻辑(try-catch 防 destroyed targetPart 导致安卓角度冻结)；OFF=旧逻辑(原始直访问)。四文件模式：StarSeaSettings.cs + INSettingsInterface.cs(Behavior 组) + INLocalizationData.json + DigitalDisplay.cs。
- [2026-08-29 19:25:49] PointChargePart 特殊电荷连接(2026-08-29最终版+8月29日晚更新)：移除SpecialChargeMaxForce，吸力恒定=SpecialChargeForce(70)；碰撞忽略改GetComponentsInChildren覆盖子对象碰撞体(伞/南瓜)；AntiTunnelCheck用AntiTunnelIgnoreColliders+引用计数跳过已连接载具(RaycastAll不尊重IgnoreCollision)；加固优先级拳套→框→其他；ShowJointConnectionCount开关控制属性面板(PropertyPanelRunning)是否显示特殊连接数/关节数；PropertyPanelRunning新增ConnectionPartner属性暴露m_connectionPartner+8月29日晚更新：移除SpecialChargeMaxForce，吸力恒定；GetComponents改GetComponentsInChildren覆盖子对象碰撞体；AntiTunnelCheck用AntiTunnelIgnoreColliders跳过已连接载具；加固优先级拳套→框→其他；ShowJointConnectionCount开关+TextMesh标签显示——ConfigurableJoint+加固+碰撞忽略)：**电荷映射**：Deg_0=正(+20)、Deg_90/Deg_270=特殊新电荷(-20)、Deg_180=原始负电荷(-20)。**两套独立系统**：Deg_0↔Deg_180 标准库仑力(吸力半径可调SpecialChargeForceRadius默认4)；Deg_90↔Deg_270 独立吸引系统。**同载具不吸引**：ElectricalSystem.cs中`charge1.ConnectedComponent==charge2.ConnectedComponent`时跳过。**连接方案**：ConfigurableJoint(x/y/zMotion=Locked, angularX/Y/Z=Free)锁定位移+自由旋转。**碰撞处理三层**：①`SetupSpecialChargeCollisionIgnore()`每帧执行(LateUpdate，无m_collisionIgnored标志)——Deg_90↔Deg_270原始碰撞体互设IgnoreCollision(电荷间不碰撞但可吸引连接)；②`TryCreateRotatableJoint`禁用原始碰撞体+加trigger SphereCollider+场景碰撞体IgnoreCollision；③`SpecialChargeAttractionCollisionIgnore`(ElectricalSystem.cs)——产生吸力时立即用**电荷实例ID**(GetInstanceID)做key去重，FindObjectsOfType遍历两载具所有部件碰撞体互设IgnoreCollision。**⚠️ ConnectedComponent ID不稳定做key**：载具合并/分离时CC ID会变，必须用实例ID。**吸合默认开启**(ChargeJointSnapEnabled默认true)。**关节连接距离**：`SpecialChargeJointDistance`(float,默认0.5)。**载具定义**：同ConnectedComponent值的部件属于同一载具；载具=Contraption中连接在一起的部件集合。**对齐关键**：sprite pivot在左上角→joint.anchor/joint.connectedAnchor设为`transform.InverseTransformPoint(renderer.bounds.center)`。**加固逻辑**：连接成功后遍历同载具部件，优先框→拳套→其他。**默认值**：SpecialChargeForce=5/SpecialChargeMaxForce=20/SpecialChargeJointBreakForce=-1/SpecialChargeJointDistance=0.5。**教训**：禁用原始碰撞体会导致电荷无法连接；CC ID做碰撞忽略key只生效一次。**星海设置**：PointCharge独立分组"点电荷"，含14项参数。**涉及文件**：PointChargePart.cs/StarSeaSettings.cs/INSettingsInterface.cs/INLocalizationData.json/ElectricalSystem.cs。
















- [2026-08-29 13:00:43] StarSeaSettings `SpecialPointChargeInvincible`(特殊点电荷无敌) bool，默认 true。四文件模式(2026-08-28重命名自PointChargeInvincible)。MakeInvincible 增加 `if (!IsSpecialCharge) return;` 守卫，仅 Deg_90/Deg_270 生效。三重保障：CreateCustomJoints + PostInitialize + LateUpdate。LateUpdate 中还调用 `SetupSpecialChargeCollisionIgnore()` 消除 Deg_90/Deg_270 间碰撞(m_collisionIgnored 标志一次性)。


- [2026-08-28 19:35:10] PointChargePart 无敌关节时序问题(2026-08-28): PartGeneratorManager 流程调用 PostInitialize 时关节尚未创建(FixedJoints=0)，且不调用 CreateCustomJoints。Contraption 主流程的 CreateCustomJoints→PostInitialize 顺序正确，但 PartGeneratorManager 走不同路径。**修复**: 三重保障——`CreateCustomJoints`(Contraption路径) + `PostInitialize`(PartGeneratorManager路径) + `LateUpdate`(逐帧检测关节出现后一次性设置)，用 `m_invincibleApplied` 标志确保只执行一次。**Why:** PostInitialize 在 PartGeneratorManager 流程中于关节创建之前调用，GetComponents&lt;FixedJoint&gt; 返回空。**How to apply:** 新增部件需要在关节创建后修改关节属性时，必须覆盖 CreateCustomJoints 并加 LateUpdate 兜底。
- [2026-08-29 11:16:57] VideoPlayer 变调控制必须用 AudioSource 输出模式：addon 的 AddonVideoPlayer.prefab 默认 `audioOutputMode=Direct`，Direct 模式下 `GetTargetAudioSource(0)` 返回 null，无法设置 pitch。INCustomBackgroundInterface.CreateVideoBackground 中已切换为 AudioSource 模式并绑定 AudioSource，ApplyVideoVolume 也改为用 `src.volume` 而非 `SetDirectAudioVolume`。**How to apply:** 任何需要通过 AudioSource.pitch 控制视频音频音调的场景，都必须先 `vp.audioOutputMode=VideoAudioOutputMode.AudioSource` + `vp.SetTargetAudioSource(0, src)`。
- [2026-08-29 11:33:45] VideoPlayer 音调控制在本项目不可行(2026-08-29已验证)：Direct 模式下 pitch 不可控；切 AudioSource 模式后 GetTargetAudioSource 确实返回非 null、pitch 也设成功，但实际音频音调无变化（addon 的 CreateVideoPlayer 可能不支持运行时切换音频模式）。用户确认无效后要求删除。**How to apply:** 不要再尝试给 INCustomBackgroundInterface 添加"音调/变调/加速变调"功能，无论是 toggle 还是输入框都不行。playbackSpeed 本身可以控制速度（Direct 模式自然变调），但独立音调控制做不到。
- [2026-08-29 18:41:31] PointChargePart 防过穿修复(2026-08-29): `INContraption.AntiTunnelCheck` 用 `Physics.RaycastAll` 检测穿透，但 raycast **不尊重** `Physics.IgnoreCollision`——即使碰撞已被忽略，射线仍会命中。修复：PointChargePart 新增 `static HashSet<Collider> AntiTunnelIgnoreColliders`（带引用计数），`TryCreateRotatableJoint` 注册两载具所有碰撞体，`DestroyConnection` 移除；`AntiTunnelCheck` 中命中碰撞体若在该集合内则 skip。**How to apply:** 任何需要"完全忽略两个物体交互"的场景，`Physics.IgnoreCollision` 不够——还需在 raycast/overlap 检测中手动排除。
- [2026-08-30 12:22:28] PointChargePart 吸力与连接规则(v6, 2026-08-30更新)：①吸力恒定=`SpecialChargeForce`(默认70)；②**只有270电荷关闭才移除吸引力**（`IsDeg270 && !IsEnabled()`），90关闭不影响吸引力但两者关闭都销毁连接；③加固拳套→框→其他；④`ShowJointConnectionCount`默认false；⑤**已去除CC Bridge**，MarkerManager保持原版；⑥触发按钮Deg_90/Deg_270=PartIndex=7(45度旋转)；⑦**SpecialChargeDefaultEnabled保留**(默认false)，PostInitialize: Deg_90始终开启，Deg_270受此设置控制；⑧点电荷设置组在机械门下面；⑨**教训**：编辑INLocalizationData.json时容易误删相邻条目（如ShowJointConnectionCount），每次修改后必须grep验证关键条目仍存在。⑩**新增ForceConnect**(默认false)：独立循环不检查类型匹配(`IsDeg90==IsDeg90`)也不检查`HasAutoJoint`，任意两个不同特殊电荷在forceRadius内就产生吸力+连接；连接前先`DestroyConnection()`销毁旧连接防止snap位移。⑪**TryCreateRotatableJoint必须检查`other.m_ringJoint!=null`**：原代码只检查`this`的m_ringJoint，不检查`other`的→已连接电荷被调用时snap代码先执行位移再return→强制位移。⑫**普通吸引循环用`||`不用`&&`**：`charge1.HasAutoJoint||charge2.HasAutoJoint`跳过已连接对，防止snap位移；`&&`会导致已连接电荷对通过检查→snap强制位移+孤立旧关节。⑬**TryAutoMatchNextTarget**：遍历所有匹配电荷跳过`HasAutoJoint`的，用于OnTouch重连失败时自动寻找下一个。⑭**重扫描逻辑**：两个都关闭(`m_needsRescan`)→OnTouch时`DestroyConnection`+清除`m_previousPartner`+`TryAutoMatchNextTarget`；只一个关闭→先尝试`m_previousPartner`再`TryAutoMatchNextTarget`。⑮**DestroyConnection改为public**供ElectricalSystem外部调用。
- [2026-08-30 16:37:55] 辅助菜单系统(v7, 2026-08-30最终版): **完全基于Unity UI**（ScreenSpaceOverlay Canvas+CanvasScaler ScaleWithScreenSize 1920×1080+GraphicRaycaster）。**⚠️项目自定义`Button`类(继承Widget)与`UnityEngine.UI.Button`同名冲突**→必须用`using UButton=UnityEngine.UI.Button`等别名；`UnityEngine.UI.Selectable`也不能用`USelectable`别名（编译器解析失败），用`UButton`默认ColorTint反馈即可。**3个子按钮**：复制/粘贴/速放（去除提示按钮因未使用）。**设置**：StarSeaSettings新增ToolBookPosX(-360)/PosY(-60)/ToolBookSize(1.0)，在星海设置→"辅助菜单"分组调整。ToolBookEnabled=true时隐藏原版toolboxButton+tutorialButton，关闭=完全隐藏无交互。**动画**：子按钮从右→左SlideTo协程动画。**教训**: replace对StarSeaSettings.cs四处编辑(字段/属性/默认/Update)必须每次验证count=1；用`using USelectable=UnityEngine.UI.Selectable`会报CS0234。
- [2026-08-30 21:27:55] **ToolBook 辅助菜单系统(2026-08-30最终版)**：锚点(1,1)右上角+拖动+INUserSettings.Save()持久化。**3列布局**：Col1(挨主按钮):子按钮垂直/Col2:删除(顶)+覆盖模式+旋转/Col3:剪切板存档按钮。**6个子按钮**：复制/粘贴/选区/撤销/速放/存剪。**覆盖模式3态**(int `ToolBookPasteOverwriteMode`，0=不覆盖有物跳/1=覆盖先RemovePartAt再BuildPart跳框WoodenFrame=5/MetalFrame=6/2=完全覆盖包括框)，点击循环。**旋转**：CyclePasteRotation()循环0°/90°/180°/270°围绕中心+flip翻转。**删除按钮**：常驻Col2顶部，选区态变"删选区"(DeleteSelection用Contraption.RemovePart)；拖拽态用于删剪切板存档。**剪切板按钮**：深灰底s_normalBg(同子按钮)，48×36尺寸，支持点击加载+拖拽粘贴(拖到撤销取消/拖到删除删存档/拖出确认)。**剪切板持久ID**：m_clipboardSlotIds+m_nextSlotId自增计数器，删除后不重编号(删B后显示1、3)；序列化到ToolBookClipboard.json含迁移兼容。**剪切板满时自动挤掉最旧**：SaveToClipboard满时RemoveAt(0)再Add。**RefreshClipboardSlots每帧重定位**：解决新建slot堆叠在(0,0)的问题。**INSelectionManager.cs**：IsFrameType(5/6)判断框→DeleteSelection()→GetRotatedClipboard()旋转逻辑。**撤销按钮红bug修复**：Update中else块重置颜色。**设置**：StarSeaSettings新增ToolBookPasteOverwriteMode(int)/ToolBookClipboardMax(int,默认3)。**UIEvent**：74-83。**涉及文件**：UIEvent.cs/StarSeaSettings.cs/INSettingsInterface.cs/INLocalizationData.json/INSelectionManager.cs/ToolBookButton.cs/GameMode.cs/IngameCamera.cs/ConstructionUI.cs。



- [2026-08-30 19:27:07] [2026-08-30] **编译计数假阳性**：`start_compilation_pipeline` 返回 `errors:1, hasErrors:true` 但 console.entries 只有1条 `OnParticleUpdateJobScheduled` Warning 无 CS error——这是项目固有现象，非代码问题。**How to apply:** 收到 `hasErrors:true` 时用 `rg "error CS"` 检查控制台 JSON 确认是否真有 CS error，不要被 errors:1 误导。
- [2026-08-30 22:51:54] 开发者交接打包: 当需要把工程传给下一个开发者时，复制到 `C:\Users\Administrator\Desktop\bpxhgc`（用户指定路径），只复制 Assets/Packages/ProjectSettings/UserSettings 四个文件夹 + .gitignore/README.md，排除 Library/Temp/Logs/Builds/screenshots/.codely-* 等缓存目录，并清理所有 .bak_* 备份文件（本项目积累了656+个）。附上 `开发说明_README.txt` 包含 Unity 版本要求、打包方法、核心代码结构、已知坑（EventSystem 冲突/Mono打包/文件上传等）。

### Reference

